export async function LocationButtons(btnClass, identificator){
    const buttons = document.querySelectorAll(`.${btnClass}`);
    buttons.forEach(btn =>{
        btn.addEventListener("click", ()=>{
            
        })
    })

}