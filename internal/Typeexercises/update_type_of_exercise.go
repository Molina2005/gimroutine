package typeexercises

import "net/http"

func (h *HandlerExercises) HandlerUpdateInfoTypeOfExercises(w http.ResponseWriter, r *http.Request) {

	// mirar si se puede hacer sin el chi.param si no solo cuando se consulte se pueda actuzliar poiendo
	// el meotod put
}
